<!DOCTYPE HTML>
<html lang="en">
    <head>
        <title><%= Application.get_env(:koueki, :instance)[:name] %></title>

        <meta charset="utf-8">
        <meta property="og:title" content="<%= Application.get_env(:koueki, :instance)[:name] %>" />
        <meta property="og:site_name" content="<%= Application.get_env(:koueki, :instance)[:name] %>" />
        <meta property="og:type" content="website" />
        <meta property="og:locale" content="en">

        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="theme-color" content="#FFFFF" />

        <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/vendor.css") %>" />
        <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/main.css") %>" />
        <link rel="icon" href="<%= Routes.static_path(@conn, "/resources/img/favicon.png") %>" type="image/png" />
    </head>
    <body>
        <div id="root"></div>

        <script>
            if (!!window.MSInputMethodContext && !!document.documentMode) {
                alert("I'd highly recommend using Firefox or Chrome. " +
                      "There is no certainty that all features will work on IE. ");
            }
        </script>

        <script src="<%= Routes.static_path(@conn, "/vendor.js") %>" defer></script>
        <script src="<%= Routes.static_path(@conn, "/main.js") %>" defer></script>

        <noscript>
            <h3>We need Javascript for any of this to even vaguely work. Enable it.</h3>
            <h4>
                Or y'know update your browser if you're using IE. 
                That should have been obvious.
            </h4>
        </noscript>
    </body>
</html>
